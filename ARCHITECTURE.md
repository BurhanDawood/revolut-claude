<!-- last-updated: 2026-08-01 -->
# ARCHITECTURE.md — Revolut X AI Portfolio Manager

> **Purpose of this document.** This is the single durable reference for how the system is built, how data flows through it, what each subsystem does, and *why* the major design decisions were made. It exists because the build history was previously fragmented across 100+ dev_log tickets, git commits, Claude's cross-session memory, and conversation logs — making "how does X work / why is it this way" a reconstruction job. Maintain this file: when a major subsystem ships or a load-bearing decision changes, update the relevant section. The live ticket board remains the `dev_log` (see §7); this document is the map, not the changelog.
>
> **Owner:** Bryan. **Last updated:** 2026-08-01. (#259 read-only backtest surface -- `get_price_series`, `get_abnormal_events`, `volatility_baseline`, `price_intraday_hourly` rollup; #261 Parts B+C `dev_recommendations` + friction aggregator; #279 `retrace_pct`/`bounce_pct` setters; #281 config merge-not-replace; #282 step 2 tier config schema + cumulative-cap validator; #278 `reset_cycle`. Earlier 2026-07-22: #262 cross-asset synthesis + conviction + thesis-nudge layer -- `get_asset_synthesis`, `asset_thesis`/`log_thesis` bear-case gate, nudge detector + passive Telegram push deliberately enabled 2026-07-19; #261 Part A Dev health snapshot `dev_health`; earlier 2026-07-01: #61 source-feeds subsystem -- YouTube/RSS content intelligence, `manage_sources` MCP tool; hardening chain #177/#180 coin_tags, #181 get_items binding, #184 YouTube transcript, #182/#183 pending-status, #185 tool retrieval; #47 B2b partial-fill pipeline; earlier 2026-06-25: #32 DND mode; #155/#156 Away Mode rung lifecycle; #150/#157 alert reminder fix; #149 batch alert reply; #159/#160 DND trough-rebuy + payment mask; #158 trade-detect prominence; #153 DND master check; #148 batch alert resolution; #161 Away Mode dust-guard exemption; #147 Away Mode cooldown notify; earlier 2026-06-23: #50C correlation guard; #144 per-rung sell_pct; #145 away_buy_usd + set_away_sell; #146 payment partial-offset; #53 concentration; #54 emotion x outcome; #51 pre-trade checklist.)

---

## 1. System Overview

A personal, single-user automated crypto portfolio manager. It monitors holdings across Revolut X and Kraken, tracks tax lots, fires price/structure/macro alerts to Telegram, executes trades through a Telegram-gated approval flow, and surfaces AI analysis and live web research. The owner is the sole decision-maker; the system is an assistant and a safety net, not an autonomous trader.

**Stack**
- **Runtime:** Node.js (v22) / Express, single `server.js` (~15,200 lines).
- **Database:** MySQL on Railway.
- **Hosting:** Railway (service `revolut-claude-production.up.railway.app`), 1 replica, US-West, deploys from GitHub on push.
- **Interfaces:** Telegram bot (alerts + trade approval), a web dashboard (`public/dashboard.js`), and an MCP server (`/mcp`) exposing tools to Claude.
- **External APIs:** Revolut X (primary exchange), Kraken (secondary), Tangem (XRP cold-storage balance), Anthropic (AI analysis + web research), Telegram Bot API, Google Drive (backups + code/read-back sync).
- **Repo:** `github.com/BurhanDawood/revolut-claude` (public). **Local:** `C:\Users\owner\revolut-claude\server.js`.

**Secrets (Railway env vars):** `REVOLUTX_API_KEY` / `REVOLUTX_PRIVATE_KEY`, `KRAKEN_API_KEY` / `KRAKEN_PRIVATE_KEY`, `ANTHROPIC_API_KEY`, Telegram bot token, `API_TOKEN` (dashboard/API auth), `BRIDGE_TOKEN`, and the Google OAuth set for backups (`GOOGLE_OAUTH_CLIENT_ID` / `_SECRET` / `_REFRESH_TOKEN` + `GDRIVE_BACKUP_FOLDER_ID`). Secrets are never pasted into chat or Cowork prompts — credential bugs are diagnosed via length/char-code checks, never by echoing values.

### 1.1 The three working threads

Development and operation are split across three Claude contexts, by design:

- **PM thread** — portfolio manager. Handles analysis, alerts, trade logging, strategy reviews, morning briefings. Reads/writes the database via MCP tools. Never edits code.
- **Dev thread** — lead developer / diagnostician. Diagnoses read-only against the live code (pulled from GitHub raw / Drive sync), writes Cowork prompts, verifies read-backs, manages the `dev_log`. Never edits live code directly.
- **Cowork** — a separate PC agent that performs the actual file edits and runs `PUSH_NOW.bat` to commit/deploy. Receives single-box prompts from the Dev thread, writes its read-back to a Google Drive folder the Dev thread reads directly.

This separation is deliberate: the Dev thread reasons and verifies but cannot push; Cowork executes but does not design. Every change passes through a written prompt + a verified read-back before deploy. The same `cross_thread` principles (auto-exec-off, never-sell-below-entry, etc.) are surfaced in both PM and Dev session briefings (see §3, decision-memory layer).

---

## 2. Data-Flow Map

### 2.1 The main monitoring loop
`checkPortfolio()` runs every 5 minutes. It pulls live tickers from Revolut X (one `/tickers` call → a price map of ~788 entries), pre-loads Kraken-only coins (GHIBLI, XPL, TAO) via Kraken, loads 24h baseline prices from `price_history`, then iterates every held/watched coin: computes % move vs 24h, skips dust (<$1 position value), and evaluates alert conditions (fixed targets, daily-move thresholds, trailing stops, auto-rules). The same loop hosts the card-payment auto-log detector (§2.7).

### 2.2 Intraday capture + abnormal-move detector (#50 Build 1 + Build 2)
`captureIntradayPrices()` runs every 2 minutes (decoupled from the 5-min loop). Captures all non-meta `coin_strategy` coins (held + watch) into `price_intraday`. This is the data clock for the abnormal-move detector. **#50 Build 2 shipped 2026-06-22:** after each capture insert, `evaluateAbnormalAlerts()` computes a per-coin volatility baseline (mean absolute %-move between consecutive 2-min captures over a 14-day lookback) and fires a Telegram alert when the latest move is both `>= ABN_K (3) × baseline` AND `>= 0.4%` (an absolute noise floor — without it, near-flat coins fire huge multiples on trivial wobble). Direction-labelled PUMP/DUMP, per-coin 30-min cooldown (`abnAlertState` Map), both directions. Alert-only, never an execution path. Live-inspectable at `GET /api/abnormal`. Validated: baselines map cleanly to coin character (BTC ~0.04%, CC ~0.06%, JTO ~0.22%).

### 2.3 Fast-cadence scan (#94 / #95 Stage 1)
`runFastScan()` runs on a tight cadence (30–60s) over a small set of flagged symbols. It checks pump-arm rules first (`checkPumpArm` — dormant detectors that arm a trailing stop on a qualifying pump, see §3 pump-loop) and then services any armed trailing stops. **Part C** services pump-loop trough-rebuy trackers (`troughTrackers.keys()`). **Part D** services standalone inverse trough-buy trackers (`standaloneTroughTrackers.keys()`). Decoupled from the 5-min loop so fast meme/volatile moves aren't missed between cycles. **#99 shipped 2026-06-20:** fast-scan is now fully dynamic — Part B evaluates `trailingStops.keys()` with a single batched `/tickers` call per 30s cycle; all armed trailing stops covered. `FAST_SCAN_SYMBOLS` retained for Part A pump-arm checks only.

### 2.4 Cron schedule (Europe/London)
- `0 0 * * *` — `recordDailyPrices` (writes daily close to `price_history`; resets daily alert tracking).
- `0 2 * * *` — prune (intraday housekeeping).
- `15 2 * * *` — prune `price_intraday` rows >30 days.
- `0 3 * * *` — `backupServerJsToDrive` (#102: nightly dated server.js snapshot → Google Drive `revolut-claude-backups`, 14-folder retention).
- `0 3 * * *` — `runReconciliation` (#55: system positions vs exchange balances vs tranche sums; flags drift).
- `30 3 * * *` — `backupDatabaseToDrive` (#12: nightly DB dump → Google Drive).
- `*/5 * * * *` — `checkMacroNews` (RSS macro/news scan).
- `5 9 * * 1` — Monday rebalancing check.
- `10 9 * * 1` — Monday weekly snapshot.
- `15 9 * * 1` — Monday rebalancing/snapshot follow-on. (**#40, 2026-06-22:** the paid `weeklyResearchSweep()` Monday cron was *removed* — research is now in-chat-first and free; see §3 Research layer. The server-side paid path stays parked for a possible future unattended refresh.)
- `15 9 * * *` — daily brief-reminder Telegram nudge (9:15 AM London): prompts Bryan to open the PM thread and say "morning brief" (the free in-chat morning-brief routine; #40).
- `0 10 * * *` — `checkIntentionOutcomes`.
- `5 10 * * *` — `checkRebalancingOutcomes`.
- `10 10 * * *` — `gradeTradeOutcomes` (#48: forward +7d/+30d grading).
- `0 8,10,12,14,16,18,20,22 * * *` — `evaluateThesisNudges` (#262 Part C: passive thesis-nudge digest, 2-hourly 08:00–22:00; gated by `thesis_nudge.enabled` — **live since 2026-07-19**).
- `5 2 * * *` — `rollupIntradayHourlyRange` (#259 Phase 3: rolls the last 72h of `price_intraday` into the long-retained `price_intraday_hourly` OHLC table. Deliberately scheduled **before** the 2:15 prune. Idempotent upsert, so overlapping runs are safe).
- Morning briefing (`5 9 * * *`) is currently **disabled** (commented), kept for manual use.

### 2.5 Alert pipeline
Conditions evaluated in the 5-min loop → if fired and not in cooldown / not acknowledged → Telegram notify. Acknowledged-alert state persists to `macro_alerts_sent` (so redeploys don't re-fire — #70). Fixed-target cooldowns are 24h. Alerts are **notify-only** for held positions; nothing auto-executes while auto-exec is disabled (current default).

### 2.6 Trade execution + approval path
A trade (from MCP `execute_kraken_trade` or the Revolut path) creates a *pending* trade → Telegram "approval needed" → owner replies 👍 / approve → execution → journal write + tranche update. **All `/api/*` write routes require `x-api-token`** (#43, fail-closed); the critical trade endpoints reject unauthenticated calls with 401 before reaching the handler. Known limitation: the approval handler tracks only the most-recent pending trade, so laddered multi-rung approvals clobber each other (#44, open).

**Auto-exec paths (multiple, all double-gated).** Several autonomous execution paths exist; each requires the global `ai_auto_execute` master ON plus a per-coin gate. (1) **Pump-loop** (#95): pump-arm → armed trailing stop → `autoExecuteSell`/`autoExecuteKrakenSell` → trough-rebuy (#130). (2) **Auto-exec trailing stop** (#93): trailing stops with `auto_execute:true` bypass Telegram approval on breach. (3) **Away Mode** (#125): fixed down-targets on away-active coins trigger AI analysis and buy if High confidence. (4) **Standalone trough-buy** (#143): `manage_alerts set_trough` arms an independent bounce-watcher. All paths carry hard entry-floor guards. The master (`ai_auto_execute.enabled`) is currently **ON**; blast-radius is controlled by `per_coin_enabled` (currently BOBA=true) for the 5-min auto-rule path — other auto-exec paths (#93, #125, #143) each have their own per-activation gates independent of `per_coin_enabled`.

### 2.7 Card-payment auto-log (#107)
Inside the 5-min balance loop, when USDT decreases with no offsetting balance increase (not a USD conversion, not a crypto buy, not recent trade-funding), the decrease is treated as a card payment: auto-logged to `trading_journal` (`source='revolut_card'`), invested capital decremented, and a one-way Telegram notice sent. A 15-minute dupe-guard prevents double-logging. Reversible anytime via `skip payment [amount]` (re-credits capital + deletes the row). This satisfies the #82 rule — capital is never *silently* mutated: every auto-log notifies and is reversible. Proven live 2026-06-17 (a real card payment auto-logged outside a redeploy window).

**Fiat withdrawal auto-log (#63, 2026-06-20).** Parallel detector: when USD (not USDT) decreases >$5 with no USDT-conversion offset and no recent crypto buy, the drop is auto-logged (`source='fiat_withdrawal'`), invested capital decremented, and Telegram notified. Reversible via `skip payment [amount]` (handler now covers both `revolut_card` and `fiat_withdrawal`). A 30-min dupe-guard on `withdrew X` prevents double-decrement if both auto-detect and manual reply fire for the same amount.

### 2.8 Reconciliation (#55)
Nightly at 3 AM: compares Revolut `/balances` (available) + Kraken balances against non-legacy tranche sums per `symbol,exchange`. Tolerance 0.5%; skips untracked/dust; tags `system > available` as possible open-order (a resting limit reserves coin). Telegram only on *new* drift. This institutionalised the manual ground-truth check that originally caught the #47 phantom-tranche bug.

---

## 3. Subsystem Map

- **MCP tools (20 active):** `get_portfolio_summary`, `get_portfolio_data`, `get_trading_data`, `get_context`, `manage_alerts`, `manage_trading`, `set_entry_price`, `execute_kraken_trade`, `set_auto_trade_rule`, `get_auto_rules`, `manage_auto_rules`, `get_prices`, `get_tranches`, `research_asset`, `set_pump_armed_rule`, `resolve_pending_trades`, `manage_sources`, `get_asset_synthesis`, `get_price_series`, `get_abnormal_events`. The tool list/count is seeded into `system_capabilities` config at boot; `project_description` roadmap updated 2026-06-20 (#2).
- **Auto-rules engine:** per-coin rules (moon-bag 25% sells at a $999 sentinel, buy-dip, etc.). 21 clean moon-bag rules currently armed. Master `ai_auto_execute` config is **disabled**; even when enabled it is railed (caps, floors, manual-only symbols) and is the rare exception, not the norm.
- **#95 pump-loop (built end-to-end, OFF by default).** A four-stage autonomous swing loop for designated coins (e.g. GHIBLI), each stage guarded:
  - **Stage 1 — pump-arm detector** (`checkPumpArm`, `pump_armed_rules` table, `set_pump_armed_rule` tool). A dormant rule arms a trailing stop when the coin pumps `arm_pump_pct` within `arm_window_min`. Arm-only; no sell.
  - **Stage 2 — entry-floor guard.** When an armed trail breaches and routes to `autoExecuteSell`/`autoExecuteKrakenSell`, a hard guard reads `entry_floor` fresh from `pump_armed_rules` and blocks the sell if `currentPrice <= floor`. Fails safe (blocks) if the lookup errors. Implements *never-sell-below-entry*.
  - **Stage 3 — single buyback rung.** After a pump-armed sell, a synthetic rule (`max_cascades:0`) feeds `cascadeRulesAfterTrade`, which creates exactly ONE buy-retrace rung (no averaging-down). The cascade engine's own guard also refuses to buy back below 95% of entry.
  - **Rinse-Repeat — continuous re-arm.** `rearmPumpLoopAfterBuyback` fires after a cascade buy fills: if enabled, it re-arms the pump rule (`armed=0`, baseline cleared) so the next qualifying pump repeats the cycle. Four guards on `pump_armed_rules`: `loop_enabled` (per-coin, default 0), the global master, a loss circuit-breaker (`loop_realized_pnl < 0` → halt), and a max-cycles cap (`cycle_count >= max_cycles`, default 10 → pause-and-ask). `loop_realized_pnl` is an approximate proxy for the circuit-breaker, not tax-grade accounting.
  - **Double-gated safety:** nothing in this loop fires unless BOTH the global `ai_auto_execute` master is ON *and* the per-coin `loop_enabled` is 1. `manage_auto_rules loop_enable/loop_disable` are the MCP setters. BOBA first live cycles running (2026-06-21+).
- **Cascade buyback engine (`cascadeRulesAfterTrade`).** After a sell, ringfences proceeds and creates a buy-retrace rule (retrace % is per-coin from `pump_armed_rules.rebuy_pct`, default 8%); after a buy, can cascade deeper — bounded by `max_cascades` and an entry-price guard (won't buy below 95% of entry). Wired into both the auto-rule executor and (via the synthetic rule) the pump-loop sell path.
- **#93 — Auto-executing trailing stop.** `manage_alerts set_trailing` accepts `auto_execute:true` + `sell_pct`. On breach, `handleTrailingStopAlert` reads live config, checks hodl_symbols and cooldown, calls `autoExecuteSell`/`autoExecuteKrakenSell` directly without Telegram approval. Single-use (trail cleared after fire). Falls through to normal analysis on config read error.
- **#24 — Per-coin auto-exec opt-in.** `ai_auto_execute` config carries `per_coin_enabled` JSON map. `shouldAutoExecute` requires `per_coin_enabled[coinBase] === true` as second gate after the master. Replaces the opt-out hodl blocklist model for the 5-min auto-rule path. Currently BOBA=true. Other auto-exec paths (#93, #125, #143) have independent per-activation gates.
- **#125 — Away Mode.** Stored as `away_mode` JSON in `system_config`. Double-gated: master ON plus per-coin away-active. Phase 2c (autonomous buy on High-confidence AI analysis of down-targets) proven live 2026-06-21 (SQD auto-bought at correct accumulation zone). Injection-resistant: coin_strategy plan outranks rung note. Session cap and getAvailableUSD check enforced. **#144** adds `sell_pct` column on `price_targets` for per-rung sell overrides; **#145** adds `away_buy_usd` param + `set_away_sell` action. Sell priority: rung sell_pct > per-coin away_sell_pct > global max_sell_pct > 25%.
- **#130 — Trough-rebuy tracker.** After pump-loop auto-sell, `armReboundTracker` stores sale price and ringfenced `sale_proceeds_usd` in `pump_armed_rules`. Fast-scan Part C tracks retrace, ratchets trough lower. On bounce above `entry_floor*(1-buyback_floor_pct/100)`: crash-safe-clear-first buy and `rearmPumpLoopAfterBuyback`. Below floor: Telegram alert and clear, Bryan decides manually.
- **#143 — Standalone inverse trough-buy.** Independent of pump-loop. Armed via `manage_alerts set_trough symbol:X-USD buy_usd:N bounce_pct:N entry_floor:N`. Stored in `standalone_trough_trackers` table and `standaloneTroughTrackers` Map. Fast-scan Part D tracks lower lows; on bounce: 4 gates (master ON, entry_floor guard, Revolut-only, getAvailableUSD), crash-safe-clear-first `placeRevolutOrder`, `source=trough_auto` journal, `[TROUGH AUTO-BOUGHT]` Telegram. Calling `set_trough` is the explicit per-coin opt-in.
- **Tranche / tax-lot tracking:** per-lot accounting supporting **US HIFO** and **UK S104**. Tranches decrement on sells; the known open work is the limit-order lifecycle + orphaned-tranche disposal (#47 Part 2).
- **Intention system:** `log_intention` records an intended trade *before* execution; outcomes are graded later (#48). Intentions match to auto-detected fills.
- **`coin_strategy` registry:** one row per coin (`status, role, theme, strategy_md, updated_at, updated_by`) — the persisted per-coin plan (thesis, alert rungs, role). Read before analysing any coin; updated in the same breath as changing an alert. Surfaced on dashboard cards.
- **Decision-memory layer (#105 — reasoning layer, both threads).** Two sibling tables capturing the *why* behind choices, distinct from the event logs (`dev_log` / `trading_journal`):
  - **`pm_decisions`** — portfolio principles (e.g. trim-into-strength, the GHIBLI pump-loop exception to don't-round-trip-memes). Surfaced in the PM `get_context` briefing with a since-last-seen digest.
  - **`dev_decisions`** — architectural principles (auto-exec-off, phased-builds, never-sell-below-entry, live-DB-is-truth, etc.), with `cross_thread`, `alternatives_rejected`, `related_dev_log`, and a `revisited` status. Surfaced in the Dev `get_trading_data include=['dev_log']` briefing with a digest. `cross_thread=1` principles also surface in the PM briefing (`crossThreadPrinciples`) so shared constraints appear in both.
  - **Recommendation engine (`pmRecommendations`)** — reasons over positions + research + principles to surface trim/rotate candidates at PM session start.
  - Logged via `log_pm_decision` / `log_dev_decision` actions on `manage_trading`. `supersedes_id` makes principle evolution traceable rather than silently overwritten.
- **USDT sweep:** optional auto-conversion of trade proceeds (currently disabled).
- **Dashboard (`public/dashboard.js`):** read views + a few control POSTs (pause/resume/sweep config). Tappable asset cards (#57). Surgical edits allowed (#66) with a `.bak` backup; `node --check` always before push.
- **Backups (3 layers):** (1) **#12** nightly DB dump → gzip → Google Drive via OAuth-delegated upload (uploads as the owner's Drive — service accounts have no Drive quota), 14-file retention, Telegram confirmation; (2) **Drive desktop sync** auto-syncs the working tree to Drive after every Cowork save; (3) **#102** nightly dated server.js snapshot → `revolut-claude-backups`, 14-folder retention.
- **Research layer (#72 — Build 1 + Build 2 shipped):** `researchAsset(symbol, triggeredBy)` + `research_asset` MCP tool — plan-aware deep web research (Anthropic web-search), evaluated against the saved `coin_strategy` plan, recommend-only. **Build 2 adds persistence + diff:** every pass is stored to the `research_history` table (thesis_status, drift_verdict, live_price, report); each new pass diffs against the prior snapshot (thesis change, new drift, >10% price move). Three entry points share one timeline: (1) **in-chat** — Claude researches with its own web_search (free, no API cost) and writes the snapshot via the `log_research` action on `manage_trading`; (2) **on-demand API** — `research_asset` tool; (3) **automated** — `weeklyResearchSweep()` cron, Mondays 9:15 AM, researches 11 held/watch coins (CC, ENA, NEAR, JTO, TON, AERO, LINK, XLM, XRP, HYPE, RSC), diffs each, sends one Telegram drift summary. Cost ~$0.22/API call → weekly sweep ~$10/mo; in-chat is free. Daily full-book deliberately not built (~$130/mo). HARD RULE: recommend-only, proposes `coin_strategy` changes, never auto-applies — Bryan approves in PM. (#90 resolved 2026-06-19: `extractDriftVerdict()` now anchors on the canonical first line `PLAN-DRIFT VERDICT: …`, fixing the false collision with section-1 thesis lines.) **#40 (2026-06-22) reframed the layer in-chat-first:** the paid Monday `weeklyResearchSweep()` was removed; routine research is done free in-chat via Claude's own web_search and stored via `log_research`. The `morning_brief_routine` preference is the playbook (zero paid API).
- **#40 — Catalyst calendar.** `catalyst_calendar` table (symbol, catalyst, catalyst_date, type, expected_impact, priced_in_risk, confidence, source, status). Written via `manage_trading log_catalyst`; read via `get_trading_data include=['catalysts']` (ordered by date, NULLs last). Pre-loaded per-coin catalysts so analysis is instant rather than cold-scrambled. Provably inert — nothing reads it for execution. (CC DTCC July-1 catalyst seeded.)
- **#49 — MSS structure tracker (alert-only).** Market Structure Shift detector — Bryan's primary exit/entry signal. `computeMssStructure()` runs at boot+120s then every 15 min: resamples `price_intraday` into 1h & 4h OHLC bars, computes an ATR proxy, runs an ATR-scaled (`MSS_ATR_K=1.5`) zigzag pivot detector, classifies HH/HL/LH/LL, derives trend + `mss_status` (intact/forming/confirmed), persists to `mss_state`. Tracks CC/NEAR/TON. **Phase B alerts:** `mssAlertState` Map (boot-loaded from `mss_state` so a redeploy never re-fires existing structure) + `evaluateMssAlerts()`. 4h is the primary actionable alert (FORMING + CONFIRMED); 1h is early-warning (CONFIRMED-only, suppressed once 4h confirms). 3h anti-flap cooldown. Forming triggers on a confirmed lower-high only. Never an execution path. Validated: NEAR 1h fired a correct CONFIRMED matching the actual selloff.
- **#50 Build 2 — Abnormal-move detector (alert-only).** See §2.2. Per-coin volatility baseline over `price_intraday`; fires on `move >= 3× baseline AND >= 0.4%`; 30-min per-coin cooldown; both PUMP/DUMP; `GET /api/abnormal` inspection surface. The early-warning layer beneath MSS and the pump-loop.
- **#32 — DND (Do Not Disturb) mode.** Extension of #95 pump-loop for opted-in coins while Bryan is unreachable. Pump 30%+ in 60 min arms a trailing stop (8% trail default); auto-sells on breach, then trough rebuy and loop re-arm via existing pump-loop infrastructure. 60-min window (not 24h baseline) — gradual grinds do not arm. 24h low floor at arm time (`MIN(price)` from `price_intraday`, stored as `pump_armed_rules.entry_floor`) so HODL coins participate without rebuying below 24h low. ae93 hodl bypass: DND coins skip the HODL gate. `configure_dnd` MCP action (sub-actions: set_eligible / set_params / activate / deactivate / status). Telegram commands: `dnd COIN1 COIN2 | dnd all | dnd off | status`. Double-gated: master ON + loop_enabled. PM controls DND directly via `configure_dnd`.
- **#155/#156 — Away Mode rung lifecycle.** After Away Mode executes (sell or buy), DELETE rung from `price_targets` + filter in-memory `priceTargets` Map. All held-off paths (no powder, low confidence, floor guard, analysis failure, session cap) leave rung live. After removal, append timestamped annotation to `coin_strategy.strategy_md` + sync `trader_profile` preference (per #111, try/catch wrapped, never blocks execution).
- **#150 — Alert reminder overhaul.** 3 reminders 60s apart then go quiet, never deleting the target and never writing a 24h mute row. Removes the auto-dismiss-after-2-reminders pattern that was silently muting CC before its July 1 pilot (#157 resolved). **#149 — Batch alert reply.** Multi-line Telegram message (`cc 2\ninj 1\nxrp 2`) resolves multiple pending alerts in one shot via `processAlertChoice` with real `sendReply` (trade-approval prompts surface). All three reply mechanisms apply to reminder alerts: (1) bare number, oldest pending; (2) coin-targeted `cc 1`; (3) multi-line batch. All cancel the reminder `setInterval` on reply.
- **#61 -- Source-feeds content intelligence (YouTube + RSS).** A pluggable content-monitoring subsystem under the intelligence-layer umbrella (#68). Two tables: `source_feeds` (id, name, type youtube|rss, url, channel_id, coin_tags JSON, last_fetched, active) and `feed_items` (id, source_id, item_id, title, published_at, url, transcript MEDIUMTEXT, analysis TEXT, coin_tags JSON, thesis_status ENUM intact|drifting|broken|neutral|pending, UNIQUE(source_id,item_id)). The `manage_sources` MCP tool exposes five actions: `add` (resolves a YouTube channel ID from URL/handle/@/user/UC form via YOUTUBE_API_KEY, or registers an RSS feed, with coin tags), `list`, `remove` (soft-deactivate), `fetch_now` (pull latest uploads/articles and analyse), `get_items` (read analysed items, filter by coin + since_days). Pipeline (`fetchAllSources`): per active source, fetch new items (YouTube last-10 uploads via the uploads playlist + page-scraped caption transcript; RSS via CDATA-aware parse), INSERT IGNORE into `feed_items`, then run `analyseSourceFeedItem` -- which pulls the saved `coin_strategy` plan for each tagged coin and calls claude-haiku-4-5 (~$0.01-0.02/item) to classify thesis impact (intact/drifting/broken/neutral) with takeaways + implication. On-demand only (no cron); analyst/news opinion is INPUT to Bryan's judgment, never an execution trigger (inherits #68 guardrails). Credentials: `YOUTUBE_API_KEY` (YouTube Data API v3) + the existing `GOOGLE_OAUTH_*` set. Hardening chain after Phase 1: coin_tags is a MySQL JSON column (MySQL2 auto-deserialises it to a JS array -- read paths must `Array.isArray()` before parsing, #177/#180); `get_items` inlines INTERVAL/LIMIT as validated integers (MySQL2 rejects bound ints there, #181); YouTube transcript extraction uses a balanced-brace scanner not a non-greedy regex (which truncated the player JSON at the first internal `};`, #184), with a title fallback when captions are unavailable; unanalysable thin items are marked `neutral` with a skip reason rather than stuck `pending` (#182/#183); the tool description is keyword-enriched for `tool_search` retrieval reliability (#185 -- the intermittent non-loading root cause is client-side deferred-tool indexing, not a server bug). RSS proven end-to-end (22 fetched / 19 analysed); YouTube transcript path fixed, awaiting a fresh-load live confirmation before the #164 strategy-extraction layer is built on top.

---

- **#262 — Cross-asset synthesis, conviction + thesis-nudge layer (PM intelligence).** Three parts, all recommend-only — the layer messages, it never trades:
  - **Part A — `get_asset_synthesis` (18th MCP tool).** Grounded cross-asset board: per asset it joins live holding + P&L (ground truth from balances), MSS structure, latest research thesis, catalysts, and the saved `coin_strategy` plan, then flags where those signals AGREE / CONFLICT / are STALE. Forms no opinions and recommends no trades — it surfaces alignment and drift only. Omit `symbol` for the full held+watchlist board.
  - **Part B — `asset_thesis` table + `log_thesis`.** Persists an explicit per-coin view (conviction high/medium/low + bull case) behind a **mandatory bear-case gate**: `bear_case`, `invalidation_triggers` and `supporting_refs` are all required (MIN_LEN 20) or the write is rejected — you cannot log a naked bull thesis. Each write auto-diffs against the previous conviction to derive `thesis_status` (`new` / `strengthened` / `weakened` / `invalidated`). Read via `get_trading_data include=['thesis']`.
  - **Part C — thesis-nudge detector + passive push.** `computeThesisNudges()` is the single shared detector used by both the read path and the cron, with four triggers: `thesis_invalidated` (sev 4), `thesis_weakened` (sev 4 if →low else 3), `thesis_structure_divergence` (high/medium conviction vs a CONFIRMED adverse 4h MSS, sev 3/2), and `regime_shift` (reuses the #50 broad-market guard, sev 3). Ranked by severity then recency; read on demand via `include=['nudges']`. `evaluateThesisNudges()` adds the passive Telegram digest, deduped per `symbol:type` via the `thesisNudgeState` Map on a cooldown. Config `thesis_nudge` in `system_config` (`enabled`, `min_severity` 3, `cooldown_min` 240) is set via `manage_trading configure_thesis_nudge`. Built OFF, then **deliberately enabled 2026-07-19** as a separate step (Build 2c) — the standing build-off-then-flip-later discipline. Quiet by default: it fires only on a real thesis transition or a broad regime shift.
- **#261 Part A — Dev health snapshot (`dev_health`).** Read-only Dev-side self-check via `get_trading_data include=['dev_health']` (opt-in; not in `fetchAll`). Four checks: **commit_sync** (`RAILWAY_GIT_COMMIT_SHA` vs GitHub HEAD from the commits atom feed → `in_sync`), **runtime** (boot time + uptime), **sensitive_config** (current-value snapshot of `ai_auto_execute`: enabled, `max_sell_pct`, `allowed_triggers`, `per_coin_enabled`, `hodl_symbols`), and **doc_staleness** (the `<!-- last-updated: YYYY-MM-DD -->` marker in this file vs `MAX(dev_log.resolved_at)`). Automates the shipped-vs-deployed and config-drift verification that was previously a manual per-session ritual. Note: inside `get_trading_data` the include-array parameter is itself named `fetch`, which shadows the global — hence `globalThis.fetch` in this block. Parts B (recommendation engine) and C (tool/workflow self-improvement) are unbuilt by design: Part A alone is the low-risk majority of the value.

- **#259 — Read-only backtest surface (PM analysis layer).** Three additions, all strictly read-only: they fire no alerts, place no orders and mutate no trading state.
  - **`get_price_series(symbol, source, start, end, resample, limit)`.** Historical price series from stored captures. `source=intraday` reads the 2-min `price_intraday` table (~30d, pruned nightly), `source=hourly` reads the long-retained `price_intraday_hourly` rollup, `source=daily` reads `price_history` closes. `resample` buckets to OHLC bars (`5m`/`1h`/`4h`/`1d`) or returns raw points; payloads are capped (default 3000, hard 6000) with a `truncated` note rather than silent loss.
  - **`get_abnormal_events(symbol, start, end, k?, abs_floor?, cooldown_min?)`.** REPLAYS the #50 detector over a historical window. This is *not* a reuse of `computeAbnormalMoves`/`abnAnalyze` — those evaluate only the *latest* move and report current state. The replay walks the series applying the same baseline+threshold maths iteratively, using an **O(n) sliding-window trailing-14d mean-abs-move** baseline proven identical to a brute-force recompute at every point (max diff 0.000e+0 across 2160 test points). Fidelity is exact because the live abnormal test uses only the baseline *mean* — `std`/`p95` are computed but unused in the decision. `k`/`abs_floor` default to live `ABN_K`/`ABN_FLOOR_PCT` and are overridable for threshold sweeps; `cooldown_min` (default 0 = raw) collapses same-direction bursts. **Fidelity caveat surfaced in the response:** intraday is pruned at 30d but the baseline needs 14d lead-in, so only the most recent ~16d are full-fidelity; each event carries `baseline_sample` and the response marks `high_fidelity_from`.
  - **`get_trading_data include=['volatility_baseline']`.** Per-symbol current 14-day baseline, std, p95 and the derived `k*baseline` threshold, plus the live `abnConfig`. Reuses `abnComputeBaseline`, so it cannot drift from the detector.
  - **`price_intraday_hourly` (Phase 3).** Long-retained hourly OHLC rollup that survives the 30d intraday prune, so the backtest window now grows indefinitely instead of capping. `UNIQUE(symbol, hour_bucket)` makes the upsert idempotent; the table is deliberately **excluded** from the prune. Seeded once at boot (23,334 buckets from the retained 30d) behind a `COUNT(*)=0` guard, then a 72h catch-up on every subsequent boot. Verified byte-identical to the raw intraday OHLC over the overlapping window.
- **#261 Parts B+C — Dev recommendations + friction aggregator** (`get_trading_data include=['dev_recommendations']`, read-only, opt-in). Part B emits ranked, severity-sorted recommendations that each carry real ticket IDs: `stale_in_progress` (sev 4), `aging_backlog` (sev 2, deliberately collapsed to ONE summary line naming the oldest three after a dry-run showed a per-ticket list would emit wallpaper), `priority_queue_stale` (sev 3, fires on queue age >30d OR >=70% of its referenced tickets already resolved), `config_risk` (sev 3), `priority_queue_missing` (sev 3). Part C aggregates `dev_log` rows with `category='friction'`: an optional `tags: a, b, c` line in the entry detail takes priority, untagged rows fall back to free-text tokenising of title+detail, and each pattern is labelled `tagged`/`inferred`/`mixed`. The hybrid was chosen only after sandbox-comparing five variants against real data — pure free-text surfaced the author's writing habits (top hit was literally the word "friction") rather than failure classes. **Convention:** always end a friction entry's detail with a kebab-case `tags:` line.

- **#282 step 2 — Tiered pump-loop CONFIG (schema + validation only; nothing executes yet).** Six columns on `pump_armed_rules`: `rule_mode` (`'single'` by default, so every existing rule behaves byte-identically), `sell_tiers`/`buy_tiers` (JSON `[[trigger_pct, size_pct], ...]`), `tier_state` (engine-owned; never written by config), `tier_cooldown_min` (15 — governs intra-cycle tier fills, while the global `cooldown_minutes` still governs re-arming), and `min_tier_usd` (2.0 dust guard). The validator **rejects** an invalid or over-cap tier set outright and writes nothing; it never silently truncates. Cumulative sell is `1 - product(1 - p/100)` because each tier sells a share of the *current* position, so two 50/50 tiers come to exactly 75% of the original — compared against the live `max_sell_pct`. It also enforces a 6-tier maximum, strictly ascending triggers, and size in (0,100]. **The five tier columns use `COALESCE(?, col)` in the upsert rather than `VALUES()`**, so a later partial call that tweaks only `trail_pct` cannot silently wipe a tier set or flip `rule_mode` back to `single` — #281's lesson applied deliberately.
- **#278 — `manage_auto_rules action='reset_cycle'`.** The missing primitive for clearing pump-loop RUNTIME state while preserving CONFIG. Clears 11 fields (`armed`, `armed_since`, `sale_price`, `sale_proceeds_usd`, `reference_base`, `retrace_gate`, `trough_low`, `trough_armed`, `baseline_price`, `baseline_at`, `tier_state`) plus any in-memory `troughTrackers` entry, and touches **zero** config columns (audited programmatically before shipping). Refuses when `sale_price IS NOT NULL` — a genuinely live tracker — unless `force=true`, so it cannot silently abandon a pending rebuy; returns `cleared_from` so every reset is auditable. It exists because `loop_disable` only sets `loop_enabled=0` and leaves the row asserting state that is no longer true. **Why fossils accumulate:** the #130 rebuy ringfences sale proceeds expecting them to remain in the account; in practice the USD is often spent or moved, so when the bounce fires GATE 3 finds the balance short, notifies, and calls `clearTroughTracker()` — which clears the five trough fields but deliberately not `armed`/`armed_since`/`sale_proceeds_usd`. That is the exact signature seen across four rows in late July ($217.81 notionally ringfenced against $0.00 actual). The ringfencing model is intentionally unchanged: notify-and-abandon is the accepted behaviour. This also unblocks #269 (auto-disarm on zero position), which can now simply call `reset_cycle`.
- **#281 — `configure_auto_execute` merge-not-replace.** The handler previously rebuilt its config object from a hardcoded 9-key whitelist, silently dropping any key not on that list — `manual_only_symbols` vanished on every partial write. Fixed by spreading `...existingCfg` first so unmanaged keys survive while explicit params still override; this generalises to any future config key. A boot seeder independently re-patches `manual_only_symbols` when absent, which is why the fix deploy also self-healed the data. **#279** separately exposed `retrace_pct`/`bounce_pct` as setters on `set_pump_armed_rule` — both columns already existed and were already read by the trough logic, but had no way to be set.

## 4. Key Design Decisions (and why)

- **MySQL on Railway, single `server.js`.** One deploy target, one process, one DB; simple to reason about and cheap to run for a single user. Not split into services because the operational surface (one owner, one portfolio) doesn't justify the complexity.
- **Auto-exec stays OFF by default.** The owner's edge is discretion; the documented weakness is rushed/FOMO decisions. The system defaults to *analyse-then-notify*, never *act*. Auto-exec, wherever enabled, is railed (caps, floors, manual-only symbols) and is the rare exception, not the norm. Enabling it is always a separate, deliberate decision in a fresh session — never bundled with the code change that built the path. The #95 pump-loop is the worked example: all four stages were built and deployed with the master OFF, so the irreversible action (the flip) stays decoupled from the build.
- **Double-gated autonomy.** Any autonomous loop requires two independent switches both ON (the global `ai_auto_execute` master AND a per-coin flag), so a single misconfiguration can't arm it. The pump-loop's `loop_enabled` is the per-coin gate.
- **Never sell below entry.** Every auto-sell path carries a hard entry-floor guard before it can fire; it fails safe (blocks) on uncertainty. A per-coin sell-floor (#45) remains the prerequisite before re-enabling any below-entry-capable trigger generally.
- **No leverage, no stop-losses.** The owner trades spot only and uses trailing stops to protect gains, never stop-losses. Generic "tighten your stops / leverage" boilerplate is wrong for this profile and must be stripped from any alert (#76).
- **Telegram-gated execution.** Every manual trade requires explicit approval. The MCP trade tool cannot fire autonomously; the API trade routes are token-protected and fail-closed.
- **Phased builds (Path C).** Large features ship as Build 1 (foundation/data clock) then Build 2+ (the intelligence on top), proving each layer live before extending. Examples: #50 (intraday capture → ATR detector), #72 (research primitive → persistence/diff), #95 (pump-arm → floor guard → rebuy → re-arm loop), #105 (decision tables → recommendation engine). Keeps each deploy small and verifiable and surfaces real cost/quality data before committing to the expensive half.
- **Decision-memory over re-derivation.** Standing principles are stored (`pm_decisions` / `dev_decisions`) and surfaced at session start so contradictions are caught *before* a build or a trade, rather than re-reasoned each session. Principle changes are superseded (traceable), never silently overwritten. The GHIBLI pump-loop exception to don't-round-trip-memes is the worked example — logged as a deliberate, bounded supersede.
- **`COIN-USD` dash format** for order symbols (Revolut convention); the price map stores both `COIN/USD` and `COIN-USD` keys.
- **Single-write tranche discipline.** Tranche side-effects should happen once, at confirmed fill — not at order placement. Double-writes at placement+fill are the root of the #47 phantom/duplicate class.
- **Crash-safe-clear-first ordering.** Any autonomous-execute path clears its in-memory Map entry (`Map.delete`, sync) *before* the irreversible action (`placeRevolutOrder` etc.). Prevents double-fire on concurrent 30s scan intervals; a crash mid-flight leaves no armed state. Trade-off: a successful action may orphan its journal write on crash — orphaned-action is manually recoverable, double-execute is not. Established in #130, applied in #143 and #125.
- **Live DB is the source of truth.** Narrative — memory, checkpoints, prior summaries — is always to be *verified* against live data, never trusted over it. When they conflict, live wins and the correction is stated explicitly. Re-pull live state immediately before any capital/state-mutating write (a stale read once caused a double-decrement).

---

## 5. Conventions (development workflow)

- **Canonical build loop (`build_workflow_canonical`).** The standing 11-step process: startup ritual → state model → pull live code (GitHub raw = code-truth) → diagnose read-only → one single-box Cowork prompt → Cowork executes + writes read-back to Drive → Claude verifies the read-back against actual file content → Bryan pushes (`PUSH_NOW.bat`) → Claude verifies boot via Railway MCP → live functional test → log + checkpoint.
- **Read-back-to-Drive workflow.** Cowork writes its read-back to a text file in the `revolut-claude-readbacks` Drive folder; the Dev thread reads it directly (no copy-paste). Bryan only says "done". Read-backs are verified against actual file content — narrative claims ("all edits landed") are never trusted.
- **Single-box Cowork prompts (#64).** Every Cowork prompt is one fenced code block (no nested fences) for one-tap copy. Edits use **search strings, not line numbers** (lines shift every push): find exact block → replace exact block. The prompt's closing checklist enumerates exactly what the read-back must contain (each edit site, `node --check` result, any greps).
- **Boot-verify via Railway MCP.** After every push: `list-deployments` → confirm new commit SUCCESS → `get-logs` types=['deploy'] → confirm Starting Container + Server running + Cron scheduled + auto-exec DISABLED + no errors + any feature-specific log line. No screenshots needed. IDs live in the `railway_ids` preference.
- **Connector-refresh rule.** A new MCP *tool*, or new *typed params* on an existing tool (e.g. a boolean/number), require Settings → Connectors → Revolut X → 'Refresh tool list' before Claude can call the new surface — deploying + clean boot is not enough (the connector caches the schema). A new *action* reusing existing params needs no refresh. (`dev_tools_protocol_addendum`.)
- **Surgical-edit policy (#66).** Targeted edits to `dashboard.js` are acceptable and often preferred over whole-file retransmission; `.bak` backup first; `node --check public/dashboard.js` must pass before every push. Dev decides atomic-vs-surgical per situation.
- **Strategy reconciliation before activation.** Before activating any new rule/strategy on a coin, reconcile against everything already in place for that coin and remove conflicts; state before→after. (A new pump-armed trailing stop once coexisted with a stale manual trailing stop and fired noise alerts — caught by chance.)
- **`node --check` before every push.** Non-negotiable. No deploy without a clean syntax check in the read-back.
- **One change at a time; never bundle across a trade-execution boundary.** Trade-path and capital-sensitive changes ship alone and are verified read-back line by line.
- **Model-usage policy (#67).** Sonnet for light work (reads, verification, small prompts, log writes); Opus for heavy work (feature design, multi-file reasoning, root-cause diagnosis); Fable reserved for the rare heaviest cases. Claude states the recommended model at session start and flags switches; the owner toggles (Claude cannot switch its own model).
- **Session continuity.** Both PM and Dev projects carry custom instructions enforcing a startup ritual (`get_context` → `get_auto_rules` → `get_trading_data include=['dev_log']`) and read a session checkpoint preference (`session_checkpoint_dev` / `_pm`) as "where we left off" — verified against live data. A checkpoint is written at session close. Build specs can be designed in one session (stored as a preference) and executed cleanly in another.
- **Chromebook base64 delivery.** Edit scripts are delivered as base64-encoded Python, chunked into 2-6 `python3 -c` one-liners using `wb`/`ab` file modes. SHA256-verified before running. Avoid double-dash in JS string content inside edit scripts — Chromebook terminal paste auto-converts them to an en-dash (0x96), corrupting the decode. Use single dash or reword.
- **Credential-bug diagnostics.** For auth failures, log only value *lengths* and last-character codes — never the secret. Mobile paste commonly introduces leading/trailing whitespace (root cause of the #12 OAuth `invalid_client` saga: a leading space on the client ID).

---

## 6. Build Timeline (selected milestones)

Reconstructed from git history and the dev_log. Not exhaustive — the `dev_log` is the full record.

- **Foundations:** Kraken monitoring + execution; Tangem XRP balance integration; trade intention system; MCP tool consolidation; Revolut X execution; tax-lot tracking (US HIFO + UK S104).
- **#43** — API auth middleware on all `/api/*` write routes (fail-closed, token-gated). *Resolved.*
- **#55** — nightly ground-truth reconciliation (positions vs exchange vs tranches). *Deployed.*
- **#48** — forward outcome grader (+7d/+30d) + journal outcome columns. *v1 shipped.*
- **#57** — coin-strategy registry + tranche GET endpoints + tappable dashboard asset cards. *Shipped.*
- **#50 Build 1** — intraday price capture (2-min cadence, 30-day prune). *Deployed/verified.*
- **#70 / #60 / #65** — alert re-fire-after-redeploy fix; baseline24h window fix; journal symbol-format fix. *Resolved.*
- **#12** — nightly DB backup to Google Drive (OAuth-delegated, gzip, 14-file retention). *Deployed/verified.*
- **#72 Build 1 + 2** — `researchAsset()` primitive + `research_asset` tool; then research persistence (`research_history`, snapshot+diff, `log_research`, `weeklyResearchSweep()`). *Deployed.*
- **#78 / #79** — front-door `README.md`; `export_dev_log` action + dated CHANGELOG snapshots. *Shipped.*
- **#82** — capital-mutation fix (USDT→USD conversions no longer corrupt invested capital). *Resolved.*
- **#71** — portfolio holdings undercount fully resolved (2026-06-18). Revolut X API returns `available` balance only; resting-limit coins were excluded. Fixed across 7 contexts (`available + reserved` = total holdings). CC gap +3,000 coins / +$467 portfolio value now correctly counted. *Resolved 2026-06-18.*
- **#91** — rolling 24h baseline fix (2026-06-18). The midnight baseline snapshot absorbed pumps already in progress (NEAR +21.67% showed as -2% to system). INNER JOIN self-join now finds `price_history` row closest to exactly 24h ago (±2h window). Continuation pumps correctly measured. *Resolved 2026-06-18.*
- **#111** — coin_strategy table ↔ preference bidirectional auto-sync (2026-06-18). Alert AI-analysis reads the `coin_strategy` TABLE; PM updates the `coin_strategy_XXX` PREFERENCE — two stores that silently drifted. Fix: `save_preference(coin_strategy_XXX)` now also upserts table `strategy_md`; `upsert_coin_strategy` mirrors back to preference. Closes the stale-plan-contradiction class. *Resolved 2026-06-18.*
- **#112** — alert direction verification (2026-06-18). Ships A+B: `fixed_price_targets` now flatMaps per-rung detail (direction/anchor/target/description); `set_target` surfaces direction auto-correction warnings. Fix #4: stale trim Telegram flag after auto-sell. Fix #3 (dashboard ▲/▼) queued. *Partially resolved 2026-06-18.*
- **#113/#114/#115/#116** — Telegram/alert quality cluster (2026-06-17–18). #113: Hold reply no longer calls `acknowledgeAlert`; ⚠️ warning labels on all mute-triggering menu options. #114: trailing stop alerts now show 4-option numbered menu (auto-ack removed). #115: `log_journal` guard — missing `trade_action` returns clear error instead of crashing. #116: stale reminder interval self-heal guard — removed rungs no longer fire zombie reminders. *All resolved.*
- **#102** — nightly dated server.js snapshot to Drive (3rd backup layer). *Shipped 2026-06-16.*
- **#107** — card-payment auto-log (USDT outflow → journal + capital decrement + reversible `skip payment`). *Shipped + proven live 2026-06-17.*
- **#105** — decision-memory layer: PM Build 1 (`pm_decisions` + `log_pm_decision` + briefing digest), PM Build 2 (`pmRecommendations` engine), Dev-side (`dev_decisions` + `log_dev_decision`), cross-thread surfacing in both briefings. *Shipped 2026-06-16.*
- **#95 — pump-loop (all four stages, OFF by default).** Stage 1 pump-arm detector → Stage 2 entry-floor guard → Stage 3 single buyback rung (`max_cascades:0`) → Rinse-Repeat continuous re-arm with 4 guards (loop_enabled, master, loss circuit-breaker, max-cycles cap). *Shipped 2026-06-16 → 2026-06-17.*
- **#109** — `.gitignore` scratch-file guards (`*.bak`, `readback-*.txt`, `.tmp.driveupload/`, `html-ids.txt`). *Shipped.*
- **#8** — lifetime economic ledger (2026-06-20). `realised_pnl_usd` column + `recordRealisedPnl()` on all 7 sell paths; `getLifetimeLedger()` + `include=['ledger']`; `buildPositions()` re-sourced from live Revolut `/balances` (fixes is_legacy=0 blindness); Kraken + Tangem merged, blended XRP entry; dashboard lifetime card; one-shot realized backfill (35 copied + 84 computed). Live: portfolio_realized −$2,578, unrealized −$5,358, lifetime −$7,936. *Resolved 2026-06-20.*
- **#94 / #99** — fast-scan quality arc (2026-06-20). #94: `runFastScan()` discarded `updateTrailingStop()` return value — breaches swallowed, never routed to `handleTrailingStopAlert`. 1-line fix. #99: replaced hardcoded `FAST_SCAN_SYMBOLS` trailing-stop loop with dynamic `trailingStops.keys()` + single batched `/tickers` call per cycle. All armed trailing stops now scanned at 30s. *Both resolved 2026-06-20.*
- **#120** — `pmRecommendations` re-sourced from `buildPositions()` (2026-06-20). Was querying `position_tranches WHERE is_legacy=0` — CC anchor (22k coins) invisible. Post-fix: CC visible, JTO ghost eliminated, HYPE +34% correct trim signal. *Resolved 2026-06-20.*
- **#87** — trailing-stop alert plan-aware (2026-06-20). `analyseTrailingStopAlert()` was the last plan-blind path. Fix mirrors A3: `getCoinContext` → role check → `coin_strategy` read → dynamic system prompt + `## SAVED PLAN` block. HODL/manual-only coins now get `[HOLD / RESET STOP]` menu only. *Resolved 2026-06-20.*
- **#31 remainder** — exit-only role gate all paths (2026-06-20). `getCoinContext` DB fallback for dead_bag/legacy_exit; gate propagated across all 6 rec/analysis paths. XPL + PYTH now get HOLD/exit messaging on all dip alerts. *Resolved 2026-06-20.*
- **#26** — USDT sweep profit basis (2026-06-20). Was computing `proceeds × sweep_pct`. Fix: optional `realisedPnlUsd` param; all 3 callers pass `(salePrice − entry) × qty`; losing trades skip sweep entirely. *Resolved 2026-06-20.*
- **#122** — rebalance journal action corrected (2026-06-20). `logRebalancePair()` set both legs to `action='transfer'` — invisible to `gradeTradeOutcomes`. Fix: sell leg → `'sell'`, buy leg → `'buy'`. *Resolved 2026-06-20.*
- **#5** — `autoLogTrade` dedup fail-safe (2026-06-20). B2a pending_orders check and CHECK 3 quantity-dedup both fell open on exception. Both now `return` on error. Root cause of AVAX double-log confirmed. *Resolved 2026-06-20.*
- **#63** — fiat withdrawal auto-log (2026-06-20). USD decrease >$5 with no USDT-conversion offset → auto-log `source='fiat_withdrawal'`, decrement capital, notify Telegram. `skip payment` handler covers both sources. `withdrew X` has 30-min dupe-guard. *Resolved 2026-06-20.*
- **#60 / #123** — baseline24h window widened (2026-06-20). Old 22–26h window excluded midnight rows at 9 AM boot → 0% moves shown. Fix: widen to 12–36h; self-join reliably finds nearest midnight row at any boot time. *Resolved 2026-06-20.*
- **#47 B2a** — limit-order fill pipeline relocated to confirmed fill (2026-06-19). `runLimitFillPipeline` at poller confirmation; `autoLogTrade` suppressed for resting limit symbols. *Shipped 2026-06-19.*
- **#52** — shadow tracker: `trade_action='pass'` added to journal enum; graded at +7d/+30d. *Shipped 2026-06-20.*
- **#136** — USD→USDT cross-cycle false fiat-withdrawal fix (2026-06-21). One-cycle pending buffer; USDT offset in next cycle clears the flag silently; else genuine withdrawal fires. *Resolved 2026-06-21.*
- **#127** — limit-order reserve triggers false withdrawal/card-payment detection (2026-06-21). Both detectors check `pending_orders` before logging; suppressed if matching resting limit found within 15 min. *Resolved 2026-06-21.*
- **#124** — `resolve_pending_trades` fallback for auto-logged rows (2026-06-21). Falls back to most recent `auto_detected`/`limit_fill` journal row within 60 min when in-memory queue is empty. *Resolved 2026-06-21.*
- **#134** — phantom CC tax_lot cleanup (2026-06-21). Two new MCP capabilities: `get_trading_data include=['tax_lots']` and `manage_trading delete_tax_lot`. *Resolved 2026-06-21.*
- **#131 / #133** — `pump_armed_rules.rebuy_pct` per-coin (default 8%, BOBA=20%); `cascadeRulesAfterTrade` reads DB. All `set_pump_armed_rule` numeric params use `z.coerce.number()`. *Resolved 2026-06-21.*
- **#129 / #126 / #138** — MCP handler destructure bugs (away_mode, manage_auto_rules loop_enable): params added to Zod schema but missing from arrow-function destructure. Fixed. Lesson: always update BOTH schema AND destructure. *Resolved 2026-06-20 to 2026-06-21.*
- **#125 — Away Mode** (all phases, 2026-06-21). Double-gated autonomous execution while unreachable. Phase 2c proven live (SQD auto-bought). Injection-resistant: plan overrides rung note. *Resolved 2026-06-21.*
- **#130 — Trough-rebuy tracker** (all phases, 2026-06-21). `sale_proceeds_usd` ringfencing; fast-scan Part C retrace-gate, trough ratchet, bounce confirm; crash-safe-clear-first buy; `rearmPumpLoopAfterBuyback`. BOBA live. *Resolved 2026-06-21.*
- **#93 — Auto-executing trailing stop** (Phases A+B, 2026-06-22). Trailing stops gain `auto_execute` + `sell_pct` + `exchange` columns. On breach: hodl check, cooldown, direct `autoExecuteSell`/`autoExecuteKrakenSell`, single-use remove. *Resolved 2026-06-22.*
- **#24 — Per-coin auto-exec opt-in** (2026-06-22). `per_coin_enabled` map in `ai_auto_execute` config; `shouldAutoExecute` requires explicit opt-in per coin. BOBA=true. Replaces opt-out blocklist. *Resolved 2026-06-22.*
- **#143 — Standalone inverse trough-buy** (Phases A+B, 2026-06-22). `manage_alerts set_trough`; `standalone_trough_trackers` table; fast-scan Part D; 4-gate execution, crash-safe-clear-first, `source=trough_auto`. *Resolved 2026-06-22.*
- **#14 / #19 / #20 / #23** — dashboard + dedup cluster (2026-06-22). #14 XRP multi-location split view (Tangem + Revolut, blended entry); #19 per-asset lifetime ledger card; #20 duplicate-journal dedup (value-tolerant 60-min match, stale-row delete on price-deviated fill); #23 saved strategy scorecards with live auto-recompute (`/api/scorecards`). *All resolved 2026-06-22.*
- **#40 — Catalyst calendar + in-chat research reframe** (2026-06-22). New `catalyst_calendar` table + `log_catalyst` action + `include=['catalysts']` read. Removed the paid `weeklyResearchSweep()` Monday cron (~$10/mo saved) — research is now in-chat-first and free; `morning_brief_routine` preference is the playbook. Daily 9:15 AM brief-reminder Telegram cron added. *Resolved 2026-06-22.*
- **#49 — MSS structure tracker** (Phases A+B, 2026-06-22). `mss_state` table + resample/ATR/zigzag/classify algorithm + `computeMssStructure()` (15-min) + `GET /api/mss`. Phase B: `mssAlertState` Map (boot-loaded anti-re-fire) + `evaluateMssAlerts()`; 4h primary (forming+confirmed), 1h early-warning (confirmed-only), 3h anti-flap cooldown. CC/NEAR/TON, alert-only. k=1.5 validated; NEAR 1h fired a correct live CONFIRMED. *Resolved 2026-06-22.*
- **#50 Build 2 — Abnormal-move detector** (Phases A+B, 2026-06-22). Per-coin volatility baseline over `price_intraday` (14-day, mean abs 2-min move); `evaluateAbnormalAlerts()` wired into `captureIntradayPrices`; fires on `>= 3× baseline AND >= 0.4%`, 30-min cooldown, both directions; `GET /api/abnormal`. Baselines validated against live data (map cleanly to coin character). *Resolved 2026-06-22.*
- **#50C** (2026-06-23): correlation guard (>=5 coins >=3% -> BROAD MARKET MOVE); individual alerts >=5%; dynamic `abnConfig` in system_config; `configure_abnormal` MCP action. *Resolved 2026-06-23.*
- **#144** (2026-06-23): `sell_pct NULL` column on `price_targets`; `set_target sell_pct:100` stores per-rung override; `tryAwayAutoSellUpTarget` reads rung->per-coin->global->25% priority. *Resolved 2026-06-23.*
- **#145** (2026-06-23): dedicated `away_buy_usd` param; `set_away_sell` action + `away_sell_pct` for symmetric Away Mode buy+sell config. Connector refresh required. *Resolved 2026-06-23.*
- **#146** (2026-06-23): payment detector partial-offset fix -- concurrent USDT->USD conversion + GBP payment in same 5-min window no longer fires inflated combined amount. *Resolved 2026-06-23.*
- **#147** (2026-06-25): Away Mode silent-cooldown gap fixed. When a down-target is hit during the 1h analysis cooldown, Telegram now notifies immediately (15-min sub-cooldown prevents spam). *Resolved 2026-06-25.*
- **#32 DND mode** (all phases, 2026-06-25). 32a: core helpers (`getDndMode/setDndMode/isDndCoin`), Telegram commands, `checkPumpArm` arm path, 24h low floor at arm time. 32b: eligible pool, `dnd all`, ae93 hodl bypass. 32c: `configure_dnd` MCP action. Bug fixes: #152 `dnd_coins` z.preprocess array coercion; #154 SELECT symbol fix (`pump_armed_rules` has no id column). Connector refresh required after 32c. *Shipped 2026-06-25.*
- **#155 / #156 — Away Mode rung lifecycle** (2026-06-25). #155: after Away Mode executes, DELETE rung from `price_targets` + filter in-memory `priceTargets` Map; held-off paths leave rung live. #156: append timestamped annotation to `coin_strategy.strategy_md` + sync `trader_profile` preference on execution (try/catch). *Resolved 2026-06-25.*
- **#150 / #157 — Alert reminder fix** (2026-06-25). 3 reminders 60s apart then quiet; never delete target, never 24h-mute. Removes auto-dismiss-after-2-reminders which silently muted CC 3-4x before July 1 pilot. Resolves #157 (CC auto-mute). *Resolved 2026-06-25.*
- **#149 — Batch alert reply** (2026-06-25). Multi-line Telegram batch reply resolves multiple pending alerts in one message. Process valid lines, report failed, show still-pending. Passes real `sendReply` so trade-approval prompts surface. All three reply mechanisms verified on all alert types including reminders. *Resolved 2026-06-25.*
- **#53** (2026-06-23): Concentration dashboard -- GET /api/concentration groups live Revolut positions by coin_strategy theme + role; dashboard 'Conc %' tab with colour-coded bar chart (green <25%, amber 25-40%, red >=40%). Feeds #51 concentration check. *Resolved 2026-06-23.*
- **#54** (2026-06-23): Emotion x outcome forward grading -- forwardSection in updateLearningModel() using outcome_7d_pct (#48 grader); emotion x action win-rate + avg forward move; followed_recommendation accuracy; surfaces in get_context learningModel when >=5 rows graded. *Resolved 2026-06-23.*
- **#51** (2026-06-23): Pre-trade checklist gate -- checkPreTrade() prepended to every execute_kraken_trade Telegram approval; 5 data-driven checks: entry/chase (>8%), churn (48h sell), catalyst (14d), concentration (#53 theme ceiling 40%), capital. Advisory only, try/catch wrapped, never blocks execution. *Resolved 2026-06-23.*
- **#147 / #159 / #160 / #158 / #153 / #148 / #161** -- Away Mode + DND + alert-UX cluster (2026-06-24 to 2026-06-26). #147 Away Mode cooldown-gap Telegram notify. #148 batch alert resolution from PM thread (`manage_alerts list_pending` + `batch_resolve`). #153 DND master-off advisory warning. #158 trade-detect prompt made visually distinct. #159 Anthropic payment masked by same-window USD->USDT conversion now detected (net-increase branch). #160 DND rebuy replaced fixed rebuy_pct with trough tracker params (dnd_retrace_pct/dnd_bounce_pct). #161 Away Mode dust-guard exemption -- DOWN (buy) fixed targets bypass the <$1 dust skip so accumulation rungs on dust/exited positions evaluate. *All resolved.*
- **#47 B2b -- limit-order partial-fill pipeline** (2026-06-26). Incremental fill tracking via `pending_orders.last_pipeline_qty` delta: partial fills, multi-batch fills, and filled-then-cancelled orphans all journal correctly at confirmed fill. Closes #47 (Part A display + B2a full-fill + B2b partial). AVAX tax-lot Part B reconstruction remains a separate PM/MCP task. *Resolved 2026-06-26.*
- **#61 -- Source-feeds content intelligence subsystem** (Phase 1 + hardening, 2026-06-30 to 2026-07-01). YouTube/RSS content monitoring: `source_feeds` + `feed_items` tables, `manage_sources` MCP tool (add/list/remove/fetch_now/get_items), transcript extraction + claude-haiku thesis analysis against `coin_strategy`. Phase 1 shipped 2026-06-30 (commit b614383). Hardening chain: #47 B2b migration follow-on (safeAddColumn -- ADD COLUMN IF NOT EXISTS unsupported on Railway MySQL); #177/#180 coin_tags JSON-column deserialisation (MySQL2 auto-parses JSON columns -- Array.isArray before parse); #181 get_items INTERVAL/LIMIT inlined (MySQL2 rejects bound ints there); #184 YouTube transcript balanced-brace extractor + title fallback (non-greedy regex truncated player JSON at internal `};`); #182/#183 thin-item pending-status fix (mark neutral + skip reason); #185 tool-description enrichment for tool_search retrieval (root cause client-side deferred-tool indexing). RSS proven end-to-end (22 fetched/19 analysed); YouTube path fixed, awaiting fresh-load live confirmation. See section 3 for the subsystem detail. *Resolved 2026-07-01 (HEAD b5f4139).*

- **#262 — Cross-asset synthesis + conviction + thesis-nudge layer** (2026-07-17 to 2026-07-19). Part A `get_asset_synthesis` grounded board (bbfbb6a); Part B `asset_thesis` + `log_thesis` mandatory bear-case gate (1975ba3, enum fix 1dce4f3); Part C nudge detector + `include=['nudges']` (a84aac9), regime-shift trigger (55fa0a2), shared `computeThesisNudges()` refactor + cron + gated push (6b35778), `configure_thesis_nudge` toggle (f6d7f58). Passive push deliberately enabled 2026-07-19. Recommend-only throughout.
- **#261 Part A — Dev health snapshot** (2026-07-21 to 2026-07-22). `get_trading_data include=['dev_health']`: commit-sync, runtime, sensitive-config snapshot, doc-staleness (2247c16; `globalThis.fetch` shadow fix 34b0548; `last-updated` marker e69577d). Read-only; Parts B/C deferred by design.

- **#261 Parts B+C — Dev recommendations + friction aggregator** (2026-07-22 to 2026-07-23). `dev_recommendations` include with five ranked recommendation types (5849eef); Part C hybrid friction aggregator after five variants were compared against real data (2b6137b). `*.md text eol=lf` in `.gitattributes` (2da357e) — a fix the friction aggregator itself surfaced, closing #270 on its second day.
- **#259 — Read-only backtest surface** (2026-07-25 to 2026-07-26). Phase 1 `get_price_series` + `volatility_baseline` (93888f4); Phase 2 `get_abnormal_events` detector replay, O(n) sliding-window baseline proven exact (0f016c1); Phase 3a `price_intraday_hourly` table + nightly rollup cron + boot backfill, 23,334 buckets seeded (9a126a3); Phase 3b `source=hourly` read path (d49c4fc). Removed the 30-day ceiling on any intraday backtest.
- **#279 — `retrace_pct`/`bounce_pct` setters** (2026-07-27, d10f91e). Two columns that already existed and were already read by the #130 trough logic finally got a way to be set.
- **#281 — `configure_auto_execute` merge-not-replace** (2026-07-30, 77850cb). One line (`...existingCfg` spread) closing a silent config-key-dropping defect; `manual_only_symbols` restored, XRP added to `hodl_symbols`.
- **#282 step 2 — Tiered pump-loop config** (2026-07-31). Migration + cumulative-cap validator (4b33495); tier params (5c9151e); validator wired with COALESCE-preserving upsert (9d2b2eb). Config and validation only — no execution path.
- **#278 — `reset_cycle`** (2026-08-01, 4c42906). Clears pump-loop runtime state while preserving config; four stale rows cleaned and $217.81 of phantom ringfencing released.

---

## 7. Open-Issues Index

The **`dev_log`** (queried via `get_trading_data include=['dev_log']`) is the live ticket board and the authoritative source for open work. High-level priority tiers (see the `dev_priority_queue` preference for the maintained ordering):

- **Tier 1 — resilience/safety:** #43 done, #12 done, #55 done, #71 done, #102 done.
- **Tier 1.5 — data cleanup:** #47 Part 2 B2a shipped 2026-06-19 (limit-order fill pipeline) — awaiting live-fill confirmation; #3 Part B AVAX tax-lot reconstruction still open. #8 lifetime ledger resolved 2026-06-20.
- **Tier 2 — core quality / intelligence layer:** #72 done; #36 plan-aware alerts done (A1/A2/A3 + #87 trailing-stop path, 2026-06-20); #90 parser done 2026-06-19; **#49 MSS tracker done 2026-06-22; #50 Build 2 abnormal-move detector done 2026-06-22; #40 catalyst calendar done 2026-06-22** (intelligence layer now substantially complete). Remaining sources: #61 YouTube, #69 X/social (both validation-first, deferred); umbrella framing #68/#39.
- **Tier 2.5 — learning loop:** #48 gradeTradeOutcomes done. #52 shadow tracker done. #54 emotion×outcome forward grading done. #51 pre-trade checklist gate done. #53 concentration dashboard done. **Learning loop cluster fully shipped 2026-06-23.**
- **Auto-exec / pump-loop:** #95 built, BOBA live cycles running. #93 auto-exec trailing stop done. #24 per-coin opt-in done. #45 per-coin sell-floor done. #125 Away Mode done (Phase 2c proven live). #144/#145 per-rung sell_pct + away_buy_usd + set_away_sell done. #130 trough-rebuy done. #143 standalone trough-buy done. **#32 DND mode done** (32a/32b/32c + #152/#154, 2026-06-25). **#155/#156 Away Mode rung lifecycle done.** Remaining: upgrade #130 rebuy to trailing trough tracker (needs PC/Cowork, spec in `rebound_tracker_spec` pref); #46 availability-gated; #33 emergency liquidation; #34 pump-capture mode.
- **Intelligence layer:** #262 cross-asset synthesis + conviction + thesis-nudge shipped 2026-07-17→19; passive push deliberately enabled 2026-07-19. #261 Part A `dev_health` shipped 2026-07-21/22 (Parts B/C open by design).
- **#263 — auto-sell polling gap + realised-P&L fill price** (open; diagnosis complete, build paused). Diagnosis found effective armed-trail sampling stretches to 90s–2.5min because of the `/tickers` price-dedup (not the 30s interval itself), and that `recordRealisedPnl` books the *scan* price rather than `average_fill_price` — market auto-sells create no `pending_orders` row, so the fill poller never corrects it. Execution-path change: design sign-off required before any build.
- **Backtest + analysis surface:** #259 shipped in full 2026-07-25/26. PM can replay the #50 detector over history and sweep thresholds; the hourly rollup means the window grows rather than capping at 30 days.
- **#282 — tiered pump loop:** steps 1 (#281 config integrity) and 2 (tier schema + validator) shipped; steps 3 (shadow) and 4 (auto-exec) **not started**. **Open question gating step 3:** the case for tiering rests on "the single-shot rebuy fired twice in 30 days". If those non-fires were mostly GATE 3 (ringfenced cash already spent) rather than the trigger being too tight, tiering the BUY leg fixes nothing — `buy_tiers` deploy a share of reserved cash that routinely is not there. Sell-side tiering stands on its own merits regardless. Resolve with evidence first. Standing cautions: ~9 free parameters fitted to 26 days of one asset, 75% cumulative sits exactly at the cap with zero headroom, four legs per cycle doubles thin-book slippage, and n=2 completed cycles is the whole evidence base.
- **Recently resolved:** #278 `reset_cycle`, #281 config merge, #279 trough setters, #270 `*.md` eol, #261 all parts, #37 build-plan meta-ticket.
- **Open, small:** #287 (`set_pump_armed_rule` response echoes params not the stored row, so COALESCE-preserved tier fields report null — cosmetic but misleading), #286 (`price_intraday` query references a non-existent `captured_at` column — silent failure in a try/catch), #268 (partial config update that does not reset the baseline every call), #269 (auto-disarm on zero position — unblocked, can call `reset_cycle`).
- **Known bugs to watch:** #76 (macro alert guardrails partial — Haiku can still produce stale narratives), #74 (context-blind ladder rec), #44 (multi-trade approval clobber — Opus, own session). #75 dashboard alerts resolved 2026-06-19. #99 dynamic fast-scan resolved 2026-06-20.
- **Cosmetic:** cron log line omits server-backup entry. project_description roadmap and tool count updated 2026-06-20 (#2).
- **Documentation:** #73 (this file). The `dev_log` export to a dated CHANGELOG (#79) guards against detail-replacement on ticket updates.

---

*This document is maintained by hand. It is a map and a rationale, not a substitute for the `dev_log` (live tickets) or git history (exact diffs). When a major subsystem ships or a load-bearing decision changes, update the affected section and the "Last updated" date.*
